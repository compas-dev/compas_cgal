# Mesh Measurements

This example demonstrates how to compute basic geometric measurements of a triangle mesh using COMPAS CGAL.

Key Features:

* Surface area via `mesh_area`
* Enclosed volume via `mesh_volume`
* Centroid via `mesh_centroid`

```python
---8<--- "docs/examples/example_measure.py"
```
